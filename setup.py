from setuptools import setup, find_packages

setup(
    name="marshall",
    version="0.2",
    packages=find_packages(), 
    author_email="gdconybear@gmail.com",
    description="A python package for the orchestration of AI agents",
)
